# 开源 Agent 发布验证（2026-09-14）

开源版从公开分支历史接收允许目录的源码快照，不合并商业分支历史。包含 Agent/CLI/HTTP、既有前端工作台、Python 模型与 worker。构建配置为 `opensource`，dev/main 保持 `hosted`。

## 全新安装和自动验证

- 在独立工作树安装依赖：Agent 使用 `pnpm install --frozen-lockfile`；前端使用 `npm ci`。前端生产构建通过。
- Agent 109 项：配置独立统计环境时 108 通过，1 项可选代码 sandbox 测试跳过。无统计环境的全新安装另外跳过真实统计 worker 用例。
- Python worker 50 项通过；统计 9 项通过，包含 138 项方法及文档示例的执行矩阵、已知数值检查，以及 6 种离散模型 × 3 种协方差设置的参数兼容回归。
- CLI 帮助和工具目录可用；Data Viz 218 个来源文件的 SHA-256 全部匹配固定版本。模板 7 实际生成 PNG/SVG/PDF，检查布局与标签；该渲染是示例，不是用户研究结果。

## 浏览器真实对话

使用独立 Agent home、生产前端及已配置的 DeepSeek 对话模型。数据为仓库明确标记的合成验收 CSV，通过真实 multipart 上传接口导入；不是从文件选择器上传，也不是伪造计算回执。

1. 首页点击“进入工作台”：无需登录，无模拟账号；侧栏显示本地工作台。账号代理返回 404。
2. 在输入框选择自由分析，从 `store-orders.csv` 开始：Agent 读取 120 行、6 列，说明订单级分析单位、回购字段含义及因果限制，持久保存计划。
3. 要求缩小计算范围后出现确认卡；在卡中提交修改，去掉额外频数步骤，旧卡显示已提交修改，新卡仅含 `describe` 和 `logit`。
4. 点击确认后刷新页面：历史、批准状态和结果恢复；实际计算完成两项，交付 HTML、CSV、esttab LaTeX/RTF、图和复现脚本。金额均值约 170.65；单因素 logit 金额系数约 0.000634、p≈0.798。这是合成数据的功能验证，不是业务证据。
5. 新建对话指定内置 Data Viz：Agent 读取技能和模板，交付 tar.gz 工程及示例预览。经前端文件接口下载后验证可解压；回复明确区分准备模板、示例预览与实际分析。下载契约测试另检查跨会话和未登记文件拒绝。

## 发现并修复

- 根目录 Python `lib/` 忽略规则误排除前端源码：补充前端例外，确保全新克隆可构建。
- 开源版不读取旧登录缓存作为本地存储范围，不生成用户或转发旧认证头；账号入口和代理按发行版隔离。
- 修正 Agent 安装文档与实际锁文件不一致。
- 离散回归的空 `cov_kwds` 泄漏至优化器并产生兼容警告：仅在非空时传递，保留稳健/聚类估计设置并增加测试。
- 加强论文解释规则：不能用峰度排除影响点；未计算的敏感性分析不猜结果；OR 点估计与区间用相同增量；单因素回归写为未调整关联。

模型的短进度仍有偶发英文，已加强语言规则，但提示词不是确定性保证。此次没有宣称完成所有主题模型、所有统计规格或付费云计算的真实用户验收。Data Viz 内置工具负责发现、读取和准备工程；自动适配/渲染仍取决于宿主提供的已授权代码 worker，也可按指南本地运行。
